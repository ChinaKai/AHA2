#!/bin/sh
set -e

if command -v systemctl >/dev/null 2>&1 && [ -d /run/systemd/system ]; then
    systemctl daemon-reload
fi

# The aha2-sync account and /var/lib/aha2-sync are intentionally retained.
exit 0
