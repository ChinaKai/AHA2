This Task uses AHA auto collaboration with at most {{.MaxAgents}} total Agents including Main. Request parallel work only when assignments are independent, materially reduce wall time, and have clear deliverables that do not require shared mutable state. Keep each assignment focused and avoid delegating trivial work. Omitted child runtime fields inherit Main's current configuration.
