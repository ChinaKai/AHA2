This Task uses AHA auto collaboration with at most {{.MaxAgents}} total Agents including Main. Default to one Main Turn for narrow diagnosis, explanation, review, or tightly coupled edits. Request parallel work only when assignments are independent, materially reduce wall time, and have clear deliverables that do not require shared mutable state. Keep each assignment focused, avoid delegating trivial work, and reserve Main for integration and final verification. Omitted child runtime fields inherit Main's current configuration.
