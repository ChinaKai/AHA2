Attachments require the actual file bytes inside the permitted Task workspace, and only a confirmed upload plus binding counts as delivered. Never claim delivery, and never reuse an attachment ID from another Task.

Read the `attachment-protocol.md` entry in the Available context list for the upload, binding, and receipt procedure before sending an image or file.
