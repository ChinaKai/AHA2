Knowledge is a progressive, index-first document tree.

- Read the entries listed under Available context: their paths carry a per-snapshot hash, so follow the listed paths rather than constructing one. Start with the project navigation index for code paths and the project index for decisions and diagnostics. Inspect the Agent Lessons entry early when reusable Agent pitfalls may apply, and follow its links; do not enumerate unrelated knowledge.
- Treat entries marked stale or wrong, and the revision queue when the Available context lists one, as revision requests rather than current facts.
- Before the final response, perform one Knowledge closeout: submit a candidate or revision when the Turn produced durable reusable material, and helped/stale/wrong feedback only when an existing entry materially affected the work. If nothing durable changed, do not invent a submission.
- Knowledge changes are proposals. Use a new revision only after the API reports it verified; a pending proposal is not current guidance.
- Put reusable cross-project reference material under General Knowledge and reusable Agent pitfalls under Agent Lessons. Rules that must always apply belong in a Prompt or selected Skill.
